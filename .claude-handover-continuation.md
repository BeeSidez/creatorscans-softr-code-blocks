# TikTok Academy build — continuation note

This is a continuation of a prior session. The original handover note is below in full. Read it, then read this short delta of what's already been verified.

---

## Verified in prior session

- Right repo confirmed: this one (`creatorscans-softr-code-blocks-`).
- `claude/create-tiktok-academy-folders-WiGIq` branch exists on origin but not yet checked out locally. Run `git checkout claude/create-tiktok-academy-folders-WiGIq` before touching anything.
- `.env` in this repo already contains `SOFTR_API_KEY=qAjFrd5QJ8YhDjRORuTs98e75`. Source it (`set -a; source .env; set +a`) before hitting the API.
- Softr API confirmed working. Base: `https://tables-api.softr.io/api/v1`. Auth header: `Softr-Api-Key: $SOFTR_API_KEY`.
- Lessons table lives in **Content Tracking App** database.
  - Database ID: `11fb1760-0482-40fb-be23-52537f179e3f`
  - Lessons table ID: `gFW6PZeT2JHVCk`
  - Course table ID: `FhDJfPPcLJEKHT`
  - Users table ID: `yZSKdACWobx3KB`
  - Events table ID: `tfAkfTmkZCkEUr`
- Lesson field IDs from prior session memory (NOT yet verified against live schema — verify before use): Name `4e8yM`, Lesson Number `42btG`, Category `jXSnc` (SELECT: LIVE/SHOP/ORGANIC/PLATFORM), Difficulty `5khor`, Description `ck1JJ`, Duration `VB4oU`, Lesson body `0wTBQ`, TOC `elIo9`, Podcast `uU6oS`.
- No `Lesson Completions` table exists yet — needs creating per the plan.
- The `itwin-course.html` reference design (the visual language to copy) is on the Desktop, not in any repo: `/Users/beverlybanahene/Desktop/Banah Media Group/Creator Scans/Creator Scans (Business)/TikTok Academy/ai-twin-guide/itwin-course.html`. Copy it into `lead-magnet/itwin/blocks/` in this repo before referencing it. Bev confirmed this is the right move.

## What to do next

1. `git checkout claude/create-tiktok-academy-folders-WiGIq`
2. Verify the folder structure described in the original handover (`tiktok-academy/lessons/`, `docs/`, etc.) actually exists on this branch. If it doesn't, ask Bev — the handover described it as already created.
3. Continue from step 1 of the original handover ("Read the live schema… regenerate `docs/softr-schema.md`").

## GitHub Actions cleanup — Bev did NOT yet confirm

Original handover proposed deleting `.github/workflows/fetch-softr-schema.yml`, `.github/workflows/fetch-full-schema.yml`, and `scripts/fetch-softr-full-schema.py`. Get explicit confirmation before removing — these were called out as destructive in the prior session.

---

# Original handover note (verbatim)

[Paste the full original handover note here — Bev should do this herself or it can be re-pasted into the new session's first message.]
