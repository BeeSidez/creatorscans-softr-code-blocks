import { useRecord, useCurrentRecordId, q } from "@/lib/datasource";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Separator } from "@/components/ui/separator";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { ExternalLink, TrendingUp, Users, Package, Video, Radio, Eye, Heart, Calendar } from "lucide-react";

const select = q.select({
  handle: "DJMLR",
  displayName: "9SavF",
  profilePicture: "nFIab",
  growthStatus: "L1M19",
  categories: "Ci9Dy",
  followers: "gqVJd",
  bio: "friU2",
  email: "3gPXC",
  profileLink: "iC0yC",
  dataPeriod: "FTDIY",
  gmv: "NLHCM",
  itemsSold: "IPbnP",
  gpm: "8IWZg",
  gmvPerCustomer: "7Yuy3",
  gmvVideo: "qFLyS",
  gmvProductCards: "s8G3N",
  gmvCategory1: "knvSu",
  gmvCategory1Pct: "FClT0",
  gmvCategory2: "oRtSw",
  gmvCategory2Pct: "JapTN",
  gmvCategory3: "qjDJt",
  gmvCategory3Pct: "vMPmx",
  gmvCategory4: "XnKZU",
  gmvCategory4Pct: "LP7x8",
  avgCommissionRate: "tDShS",
  products: "qZQWl",
  brandCollaborations: "QbPaf",
  productPrice: "DrU4d",
  videoGpm: "pX3Mi",
  videos: "j29Kz",
  avgVideoViews: "irsqY",
  avgVideoEngagement: "7MZyr",
  liveGpm: "flKa9",
  liveStreams: "hkfxc",
  avgLiveViews: "Lklp6",
  avgLiveEngagement: "FsKSB",
  female: "8oWzb",
  male: "B1cDT",
  age1824: "iLAHe",
  age2534: "IwCFt",
  age3544: "LCYcc",
  age4554: "xVm1S",
  age55plus: "SSUan",
  topLocation1: "AgvYQ",
  topLocation2: "Pim79",
  topLocation3: "ovoWe",
  topLocation4: "pRBW1",
  topLocation5: "ObraO",
  video1Title: "HCuNG",
  video1Views: "zpc2Z",
  video1Likes: "fI0hf",
  video1Date: "MIIdr",
  video1Url: "mXL9B",
  video2Title: "OVfPs",
  video2Views: "FMDto",
  video2Likes: "ABf4A",
  video2Date: "xD56W",
  video2Url: "LhwgP",
  video3Title: "BiO4j",
  video3Views: "VLtCq",
  video3Likes: "siEfP",
  video3Date: "Te4lu",
  video3Url: "1cqH6",
});

function getVideoEmbedUrl(url: string | null | undefined): string | null {
  if (!url) return null;
  
  // TikTok
  const tiktokMatch = url.match(/tiktok\.com\/@[^\/]+\/video\/(\d+)/);
  if (tiktokMatch) {
    return `https://www.tiktok.com/embed/v2/${tiktokMatch[1]}`;
  }
  
  // YouTube
  const youtubeMatch = url.match(/(?:youtube\.com\/watch\?v=|youtu\.be\/)([^&]+)/);
  if (youtubeMatch) {
    return `https://www.youtube.com/embed/${youtubeMatch[1]}`;
  }
  
  // Instagram
  const instagramMatch = url.match(/instagram\.com\/(?:p|reel)\/([^\/]+)/);
  if (instagramMatch) {
    return `https://www.instagram.com/p/${instagramMatch[1]}/embed`;
  }
  
  return null;
}

export default function Block() {
  const recordId = useCurrentRecordId();
  const { data, status } = useRecord({ recordId, select });

  if (status === "pending") {
    return (
      <div className="container py-12">
        <div className="content flex items-center justify-center min-h-[400px]">
          <div className="text-muted-foreground">Loading dashboard...</div>
        </div>
      </div>
    );
  }

  if (status === "error" || !data) {
    return (
      <div className="container py-12">
        <div className="content flex items-center justify-center min-h-[400px]">
          <div className="text-destructive">Error loading dashboard data</div>
        </div>
      </div>
    );
  }

  const profilePic = Array.isArray(data.fields.profilePicture) && data.fields.profilePicture.length > 0 
    ? data.fields.profilePicture[0].url 
    : null;

  const growthStatus = data.fields.growthStatus?.label || null;
  const dataPeriod = data.fields.dataPeriod?.label || null;

  const video1EmbedUrl = getVideoEmbedUrl(data.fields.video1Url);
  const video2EmbedUrl = getVideoEmbedUrl(data.fields.video2Url);
  const video3EmbedUrl = getVideoEmbedUrl(data.fields.video3Url);

  return (
    <div className="container py-8">
      <div className="content space-y-8">
        {/* Profile Header */}
        <Card>
          <CardContent className="pt-6">
            <div className="flex flex-col md:flex-row gap-6 items-start">
              <Avatar className="h-24 w-24">
                <AvatarImage src={profilePic || undefined} alt={data.fields.displayName || data.fields.handle || "Creator"} />
                <AvatarFallback className="text-2xl">{(data.fields.displayName || data.fields.handle || "C")[0].toUpperCase()}</AvatarFallback>
              </Avatar>
              <div className="flex-1 space-y-2">
                <div className="flex flex-wrap items-center gap-3">
                  <h1 className="font-heading text-3xl">{data.fields.displayName || data.fields.handle}</h1>
                  {growthStatus && <Badge variant="secondary">{growthStatus}</Badge>}
                </div>
                {data.fields.handle && <p className="text-muted-foreground">@{data.fields.handle}</p>}
                {data.fields.bio && <p className="text-sm mt-2">{data.fields.bio}</p>}
                <div className="flex flex-wrap gap-4 text-sm text-muted-foreground mt-3">
                  {data.fields.categories && <span>📂 {data.fields.categories}</span>}
                  {data.fields.followers && <span>👥 {data.fields.followers} followers</span>}
                  {data.fields.profileLink && (
                    <a href={data.fields.profileLink} target="_blank" rel="noopener noreferrer" className="flex items-center gap-1 hover:text-foreground">
                      <ExternalLink className="h-3 w-3" /> Profile
                    </a>
                  )}
                </div>
              </div>
            </div>
          </CardContent>
        </Card>

        {dataPeriod && (
          <div className="text-sm text-muted-foreground">
            📊 Data Period: {dataPeriod}
          </div>
        )}

        {/* Key Metrics */}
        <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
          <Card>
            <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
              <CardTitle className="text-sm font-medium">Total GMV</CardTitle>
              <TrendingUp className="h-4 w-4 text-muted-foreground" />
            </CardHeader>
            <CardContent>
              <div className="text-2xl font-bold">{data.fields.gmv || "—"}</div>
              {data.fields.gpm !== null && <p className="text-xs text-muted-foreground mt-1">GPM: {typeof data.fields.gpm === 'number' ? `£${data.fields.gpm.toFixed(2)}` : data.fields.gpm}</p>}
            </CardContent>
          </Card>

          <Card>
            <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
              <CardTitle className="text-sm font-medium">Items Sold</CardTitle>
              <Package className="h-4 w-4 text-muted-foreground" />
            </CardHeader>
            <CardContent>
              <div className="text-2xl font-bold">{data.fields.itemsSold || "—"}</div>
              {data.fields.products !== null && <p className="text-xs text-muted-foreground mt-1">{data.fields.products} products</p>}
            </CardContent>
          </Card>

          <Card>
            <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
              <CardTitle className="text-sm font-medium">Videos</CardTitle>
              <Video className="h-4 w-4 text-muted-foreground" />
            </CardHeader>
            <CardContent>
              <div className="text-2xl font-bold">{data.fields.videos !== null ? data.fields.videos : "—"}</div>
              {data.fields.avgVideoViews !== null && <p className="text-xs text-muted-foreground mt-1">Avg {data.fields.avgVideoViews.toLocaleString()} views</p>}
            </CardContent>
          </Card>

          <Card>
            <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
              <CardTitle className="text-sm font-medium">Live Streams</CardTitle>
              <Radio className="h-4 w-4 text-muted-foreground" />
            </CardHeader>
            <CardContent>
              <div className="text-2xl font-bold">{data.fields.liveStreams !== null ? data.fields.liveStreams : "—"}</div>
              {data.fields.avgLiveViews && <p className="text-xs text-muted-foreground mt-1">Avg {data.fields.avgLiveViews} views</p>}
            </CardContent>
          </Card>
        </div>

        {/* Sales Performance */}
        <div className="grid gap-4 md:grid-cols-2">
          <Card>
            <CardHeader>
              <CardTitle>Sales Channels</CardTitle>
              <CardDescription>GMV breakdown by channel</CardDescription>
            </CardHeader>
            <CardContent className="space-y-3">
              {data.fields.gmvVideo && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">Video</span>
                  <span className="font-medium">{data.fields.gmvVideo}</span>
                </div>
              )}
              {data.fields.gmvProductCards && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">Product Cards</span>
                  <span className="font-medium">{data.fields.gmvProductCards}</span>
                </div>
              )}
            </CardContent>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>Performance Metrics</CardTitle>
              <CardDescription>Key performance indicators</CardDescription>
            </CardHeader>
            <CardContent className="space-y-3">
              {data.fields.gmvPerCustomer !== null && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">GMV per Customer</span>
                  <span className="font-medium">{typeof data.fields.gmvPerCustomer === 'number' ? `£${data.fields.gmvPerCustomer.toFixed(2)}` : data.fields.gmvPerCustomer}</span>
                </div>
              )}
              {data.fields.avgCommissionRate && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">Avg Commission Rate</span>
                  <span className="font-medium">{data.fields.avgCommissionRate}</span>
                </div>
              )}
              {data.fields.brandCollaborations !== null && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">Brand Collaborations</span>
                  <span className="font-medium">{data.fields.brandCollaborations}</span>
                </div>
              )}
            </CardContent>
          </Card>
        </div>

        {/* Category Performance */}
        {(data.fields.gmvCategory1 || data.fields.gmvCategory2 || data.fields.gmvCategory3 || data.fields.gmvCategory4) && (
          <Card>
            <CardHeader>
              <CardTitle>Top Categories by GMV</CardTitle>
            </CardHeader>
            <CardContent className="space-y-3">
              {data.fields.gmvCategory1 && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">{data.fields.gmvCategory1}</span>
                  <span className="font-medium">{data.fields.gmvCategory1Pct || "—"}</span>
                </div>
              )}
              {data.fields.gmvCategory2 && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">{data.fields.gmvCategory2}</span>
                  <span className="font-medium">{data.fields.gmvCategory2Pct || "—"}</span>
                </div>
              )}
              {data.fields.gmvCategory3 && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">{data.fields.gmvCategory3}</span>
                  <span className="font-medium">{data.fields.gmvCategory3Pct || "—"}</span>
                </div>
              )}
              {data.fields.gmvCategory4 && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">{data.fields.gmvCategory4}</span>
                  <span className="font-medium">{data.fields.gmvCategory4Pct || "—"}</span>
                </div>
              )}
            </CardContent>
          </Card>
        )}

        {/* Audience Demographics */}
        <div className="grid gap-4 md:grid-cols-2">
          <Card>
            <CardHeader>
              <CardTitle>Audience Demographics</CardTitle>
            </CardHeader>
            <CardContent className="space-y-4">
              {(data.fields.female || data.fields.male) && (
                <div>
                  <p className="text-sm font-medium mb-2">Gender</p>
                  <div className="space-y-2">
                    {data.fields.female && (
                      <div className="flex justify-between">
                        <span className="text-sm text-muted-foreground">Female</span>
                        <span className="text-sm font-medium">{data.fields.female}</span>
                      </div>
                    )}
                    {data.fields.male && (
                      <div className="flex justify-between">
                        <span className="text-sm text-muted-foreground">Male</span>
                        <span className="text-sm font-medium">{data.fields.male}</span>
                      </div>
                    )}
                  </div>
                </div>
              )}
              <Separator />
              {(data.fields.age1824 || data.fields.age2534 || data.fields.age3544 || data.fields.age4554 || data.fields.age55plus) && (
                <div>
                  <p className="text-sm font-medium mb-2">Age Groups</p>
                  <div className="space-y-2">
                    {data.fields.age1824 && (
                      <div className="flex justify-between">
                        <span className="text-sm text-muted-foreground">18-24</span>
                        <span className="text-sm font-medium">{data.fields.age1824}</span>
                      </div>
                    )}
                    {data.fields.age2534 && (
                      <div className="flex justify-between">
                        <span className="text-sm text-muted-foreground">25-34</span>
                        <span className="text-sm font-medium">{data.fields.age2534}</span>
                      </div>
                    )}
                    {data.fields.age3544 && (
                      <div className="flex justify-between">
                        <span className="text-sm text-muted-foreground">35-44</span>
                        <span className="text-sm font-medium">{data.fields.age3544}</span>
                      </div>
                    )}
                    {data.fields.age4554 && (
                      <div className="flex justify-between">
                        <span className="text-sm text-muted-foreground">45-54</span>
                        <span className="text-sm font-medium">{data.fields.age4554}</span>
                      </div>
                    )}
                    {data.fields.age55plus && (
                      <div className="flex justify-between">
                        <span className="text-sm text-muted-foreground">55+</span>
                        <span className="text-sm font-medium">{data.fields.age55plus}</span>
                      </div>
                    )}
                  </div>
                </div>
              )}
            </CardContent>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>Top Locations</CardTitle>
            </CardHeader>
            <CardContent className="space-y-2">
              {data.fields.topLocation1?.label && <div className="text-sm">1. {data.fields.topLocation1.label}</div>}
              {data.fields.topLocation2?.label && <div className="text-sm">2. {data.fields.topLocation2.label}</div>}
              {data.fields.topLocation3 && <div className="text-sm">3. {data.fields.topLocation3}</div>}
              {data.fields.topLocation4 && <div className="text-sm">4. {data.fields.topLocation4}</div>}
              {data.fields.topLocation5 && <div className="text-sm">5. {data.fields.topLocation5}</div>}
            </CardContent>
          </Card>
        </div>

        {/* Top Videos with Embeds */}
        {(data.fields.video1Title || data.fields.video2Title || data.fields.video3Title) && (
          <Card>
            <CardHeader>
              <CardTitle>Top Videos with Products</CardTitle>
            </CardHeader>
            <CardContent>
              <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
                {data.fields.video1Title && (
                  <div className="space-y-3">
                    {video1EmbedUrl ? (
                      <div className="aspect-[9/16] w-full rounded-lg overflow-hidden bg-muted">
                        <iframe
                          src={video1EmbedUrl}
                          className="w-full h-full"
                          allowFullScreen
                          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                        />
                      </div>
                    ) : data.fields.video1Url ? (
                      <a href={data.fields.video1Url} target="_blank" rel="noopener noreferrer" className="block aspect-[9/16] w-full rounded-lg bg-muted flex items-center justify-center hover:bg-muted/80">
                        <Video className="h-12 w-12 text-muted-foreground" />
                      </a>
                    ) : (
                      <div className="aspect-[9/16] w-full rounded-lg bg-muted flex items-center justify-center">
                        <Video className="h-12 w-12 text-muted-foreground" />
                      </div>
                    )}
                    <div className="space-y-2">
                      <h4 className="font-medium text-sm line-clamp-2">{data.fields.video1Title}</h4>
                      <div className="flex flex-wrap gap-3 text-xs text-muted-foreground">
                        {data.fields.video1Views && (
                          <span className="flex items-center gap-1">
                            <Eye className="h-3 w-3" /> {data.fields.video1Views}
                          </span>
                        )}
                        {data.fields.video1Likes && (
                          <span className="flex items-center gap-1">
                            <Heart className="h-3 w-3" /> {data.fields.video1Likes}
                          </span>
                        )}
                      </div>
                      {data.fields.video1Date && (
                        <div className="text-xs text-muted-foreground flex items-center gap-1">
                          <Calendar className="h-3 w-3" /> {new Date(data.fields.video1Date).toLocaleDateString()}
                        </div>
                      )}
                    </div>
                  </div>
                )}

                {data.fields.video2Title && (
                  <div className="space-y-3">
                    {video2EmbedUrl ? (
                      <div className="aspect-[9/16] w-full rounded-lg overflow-hidden bg-muted">
                        <iframe
                          src={video2EmbedUrl}
                          className="w-full h-full"
                          allowFullScreen
                          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                        />
                      </div>
                    ) : data.fields.video2Url ? (
                      <a href={data.fields.video2Url} target="_blank" rel="noopener noreferrer" className="block aspect-[9/16] w-full rounded-lg bg-muted flex items-center justify-center hover:bg-muted/80">
                        <Video className="h-12 w-12 text-muted-foreground" />
                      </a>
                    ) : (
                      <div className="aspect-[9/16] w-full rounded-lg bg-muted flex items-center justify-center">
                        <Video className="h-12 w-12 text-muted-foreground" />
                      </div>
                    )}
                    <div className="space-y-2">
                      <h4 className="font-medium text-sm line-clamp-2">{data.fields.video2Title}</h4>
                      <div className="flex flex-wrap gap-3 text-xs text-muted-foreground">
                        {data.fields.video2Views && (
                          <span className="flex items-center gap-1">
                            <Eye className="h-3 w-3" /> {data.fields.video2Views}
                          </span>
                        )}
                        {data.fields.video2Likes && (
                          <span className="flex items-center gap-1">
                            <Heart className="h-3 w-3" /> {data.fields.video2Likes}
                          </span>
                        )}
                      </div>
                      {data.fields.video2Date && (
                        <div className="text-xs text-muted-foreground flex items-center gap-1">
                          <Calendar className="h-3 w-3" /> {new Date(data.fields.video2Date).toLocaleDateString()}
                        </div>
                      )}
                    </div>
                  </div>
                )}

                {data.fields.video3Title && (
                  <div className="space-y-3">
                    {video3EmbedUrl ? (
                      <div className="aspect-[9/16] w-full rounded-lg overflow-hidden bg-muted">
                        <iframe
                          src={video3EmbedUrl}
                          className="w-full h-full"
                          allowFullScreen
                          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                        />
                      </div>
                    ) : data.fields.video3Url ? (
                      <a href={data.fields.video3Url} target="_blank" rel="noopener noreferrer" className="block aspect-[9/16] w-full rounded-lg bg-muted flex items-center justify-center hover:bg-muted/80">
                        <Video className="h-12 w-12 text-muted-foreground" />
                      </a>
                    ) : (
                      <div className="aspect-[9/16] w-full rounded-lg bg-muted flex items-center justify-center">
                        <Video className="h-12 w-12 text-muted-foreground" />
                      </div>
                    )}
                    <div className="space-y-2">
                      <h4 className="font-medium text-sm line-clamp-2">{data.fields.video3Title}</h4>
                      <div className="flex flex-wrap gap-3 text-xs text-muted-foreground">
                        {data.fields.video3Views && (
                          <span className="flex items-center gap-1">
                            <Eye className="h-3 w-3" /> {data.fields.video3Views}
                          </span>
                        )}
                        {data.fields.video3Likes && (
                          <span className="flex items-center gap-1">
                            <Heart className="h-3 w-3" /> {data.fields.video3Likes}
                          </span>
                        )}
                      </div>
                      {data.fields.video3Date && (
                        <div className="text-xs text-muted-foreground flex items-center gap-1">
                          <Calendar className="h-3 w-3" /> {new Date(data.fields.video3Date).toLocaleDateString()}
                        </div>
                      )}
                    </div>
                  </div>
                )}
              </div>
            </CardContent>
          </Card>
        )}

        {/* Content Performance */}
        <div className="grid gap-4 md:grid-cols-2">
          <Card>
            <CardHeader>
              <CardTitle>Video Performance</CardTitle>
            </CardHeader>
            <CardContent className="space-y-3">
              {data.fields.videoGpm !== null && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">Video GPM</span>
                  <span className="font-medium">{typeof data.fields.videoGpm === 'number' ? `£${data.fields.videoGpm.toFixed(2)}` : data.fields.videoGpm}</span>
                </div>
              )}
              {data.fields.avgVideoEngagement && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">Avg Engagement</span>
                  <span className="font-medium">{data.fields.avgVideoEngagement}</span>
                </div>
              )}
            </CardContent>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>Live Stream Performance</CardTitle>
            </CardHeader>
            <CardContent className="space-y-3">
              {data.fields.liveGpm !== null && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">Live GPM</span>
                  <span className="font-medium">{typeof data.fields.liveGpm === 'number' ? `£${data.fields.liveGpm}` : data.fields.liveGpm}</span>
                </div>
              )}
              {data.fields.avgLiveEngagement && (
                <div className="flex justify-between items-center">
                  <span className="text-sm">Avg Engagement</span>
                  <span className="font-medium">{data.fields.avgLiveEngagement}</span>
                </div>
              )}
            </CardContent>
          </Card>
        </div>
      </div>
    </div>
  );
}
