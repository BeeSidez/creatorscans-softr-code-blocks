import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { useState } from "react";
import { ArrowRight, Check } from "lucide-react";

// ─── Icon asset URLs ─────────────────────────────────────────────
const ICON = {
  money: "https://res.cloudinary.com/dmrekfwpj/image/upload/v1777045486/money_vewclt.png",
  community: "https://res.cloudinary.com/dmrekfwpj/image/upload/v1777045485/community_caug7o.png",
  creative_feedback: "https://res.cloudinary.com/dmrekfwpj/image/upload/v1777045485/creative_feedback_zb1so7.png",
  commission: "https://res.cloudinary.com/dmrekfwpj/image/upload/v1777045485/commission_ly9eje.png",
  recreate: "https://res.cloudinary.com/dmrekfwpj/image/upload/v1776198110/Recreate_jz9b7s.png",
  strategy: "https://res.cloudinary.com/dmrekfwpj/image/upload/v1777045488/strategy_mwnadb.png",
  communication: "https://res.cloudinary.com/dmrekfwpj/image/upload/v1777045485/communication_wjncjm.png",
  rocket: "https://res.cloudinary.com/dmrekfwpj/image/upload/v1777045487/rocket_fdv1na.png",
  opportunities: "https://res.cloudinary.com/dmrekfwpj/image/upload/v1777045486/oppourtunities_swbrli.png",
  tiktok_academy: "https://res.cloudinary.com/dmrekfwpj/image/upload/v1777045488/tiktok_accademy_e2jnyk.png",
};

// ─── Shop creator features ───────────────────────────────────────
const SHOP_FEATURES = [
  {
    icon: ICON.money,
    tag: "Paid upfront",
    title: "Cash + Commission Deals",
    description: "Every campaign you join pays upfront cash plus commission on sales. You earn the moment you post — and keep earning every time your video converts.",
    bullets: [
      "Guaranteed cash on delivery",
      "Commission on every Shop sale",
      "No unpaid 'exposure' gigs",
    ],
    stat: { value: "£840", label: "avg creator payout" },
  },
  {
    icon: ICON.commission,
    tag: "Exclusive access",
    title: "Higher Commission Rates",
    description: "As an official TikTok Shop Partner we unlock exclusive brand deals with commission rates 5–15% higher than the open marketplace.",
    bullets: [
      "5–15% higher than marketplace",
      "Exclusive partner-only brands",
      "Priority on new launches",
    ],
    stat: { value: "+12%", label: "avg rate uplift" },
  },
  {
    icon: ICON.opportunities,
    tag: "Free to keep",
    title: "Free Product Samples",
    description: "Brands send you the product before you film. You try it, keep it, and only post if it's a genuine fit — no obligation, no returns.",
    bullets: [
      "Products shipped direct to you",
      "Yours to keep, no strings",
      "Only post what you actually like",
    ],
    stat: { value: "2,100+", label: "samples sent Q3" },
  },
  {
    icon: ICON.strategy,
    tag: "Fair pricing",
    title: "GMV-Matched Rates",
    description: "Your rate scales with the GMV you generate. Drive more sales, unlock higher tiers. No more flat fees that ignore how much you actually move.",
    bullets: [
      "Tiered rate card based on GMV",
      "Automatic tier-ups each quarter",
      "Transparent performance pricing",
    ],
    stat: { value: "+3.1×", label: "vs flat-fee avg" },
  },
  {
    icon: ICON.tiktok_academy,
    tag: "6 courses · 40 lessons",
    title: "Shop Academy",
    description: "Six full courses covering Shop Foundations, Shop Mastery, LIVE, and organic content. Each lesson has a podcast, video, lesson plan, PDF, quiz, and certificate.",
    bullets: [
      "Shop Foundations + Mastery",
      "Podcast + video + PDF per lesson",
      "6 certificates on completion",
    ],
    stat: { value: "6", label: "certificates available" },
  },
  {
    icon: ICON.rocket,
    tag: "Level up",
    title: "Career Growth Path",
    description: "Start as a creator and grow into a Creator Manager. Build your own roster, earn override income, and develop leadership skills inside the network.",
    bullets: [
      "Creator → Creator Manager",
      "Build and lead your own team",
      "Override income on your roster",
    ],
    stat: { value: "24", label: "managers promoted" },
  },
];

// ─── LIVE creator features ───────────────────────────────────────
const LIVE_FEATURES = [
  {
    icon: ICON.money,
    tag: "Paid LIVEs",
    title: "Sponsored LIVE Gigs",
    description: "Get booked for paid LIVE campaigns where brands pay you to feature their products during your stream. Guaranteed airtime, guaranteed pay.",
    bullets: [
      "Guaranteed base fee per LIVE",
      "Commission on in-stream sales",
      "Briefed product placements",
    ],
    stat: { value: "£420", label: "avg LIVE booking" },
  },
  {
    icon: ICON.opportunities,
    tag: "Members only",
    title: "Exclusive LIVE Opportunities",
    description: "Access tournaments, exclusive brand campaigns, and traffic-boost requests only available to Creator Scans network members.",
    bullets: [
      "Exclusive brand tournaments",
      "Bonus pools + prize payouts",
      "Traffic boost allocations",
    ],
    stat: { value: "18", label: "events this quarter" },
  },
  {
    icon: ICON.communication,
    tag: "Priority line",
    title: "Direct TikTok Support",
    description: "Through our official Creator Network status we open support tickets directly for account issues, unbans, verification, and violations — fast-tracked.",
    bullets: [
      "Fast-track unban support",
      "Verification + account issues",
      "Violation ticket escalation",
    ],
    stat: { value: "<24h", label: "avg resolution" },
  },
  {
    icon: ICON.creative_feedback,
    tag: "Personalised",
    title: "LIVE Strategy & Reviews",
    description: "Our team reviews your past LIVE sessions and gives you detailed feedback plus proven scripts to maximise engagement and in-stream sales.",
    bullets: [
      "Session-by-session review",
      "Monetisation scripts + prompts",
      "Conversion optimisation tips",
    ],
    stat: { value: "7/wk", label: "reviews per creator" },
  },
  {
    icon: ICON.tiktok_academy,
    tag: "6 courses · 40 lessons",
    title: "LIVE Academy",
    description: "LIVE Foundations and LIVE Mastery plus four supporting courses on Shop and organic. Each lesson includes podcast, video, lesson plan, PDF, quiz, and certificate.",
    bullets: [
      "LIVE Foundations + Mastery",
      "Podcast + video + PDF per lesson",
      "6 certificates on completion",
    ],
    stat: { value: "6", label: "certificates available" },
  },
  {
    icon: ICON.rocket,
    tag: "Level up",
    title: "Career Growth Path",
    description: "Start as a LIVE creator and grow into a Creator Manager role. Build your own roster of streamers, earn override income, and lead.",
    bullets: [
      "Creator → Creator Manager",
      "Build and lead your own team",
      "Override income on your roster",
    ],
    stat: { value: "24", label: "managers promoted" },
  },
];

// ─── Creator Features badge ──────────────────────────────────────
function CreatorFeaturesBadge() {
  return (
    <Badge
      variant="secondary"
      className="bg-white/5 border-white/20 backdrop-blur-sm px-4 py-2 text-xs font-bold inline-flex items-center gap-2 rounded-full uppercase tracking-[2px]"
      style={{ color: "#ffb3c1" }}
    >
      <span className="rounded-full" style={{ width: 6, height: 6, background: "#fe2c55" }} />
      Creator Features
    </Badge>
  );
}

// ─── Tab pill ────────────────────────────────────────────────────
function TabPill({ active, onClick, label, accent }) {
  return (
    <button
      onClick={onClick}
      className="relative transition-all duration-300 rounded-full font-bold uppercase tracking-[2px]"
      style={{
        padding: "14px 28px",
        fontSize: 12,
        border: active ? `1px solid ${accent}` : "1px solid rgba(255,255,255,0.12)",
        background: active ? `${accent}22` : "rgba(255,255,255,0.03)",
        color: active ? accent : "rgba(255,255,255,0.55)",
      }}
    >
      <span className="inline-flex items-center gap-2">
        <span className="rounded-full" style={{ width: 6, height: 6, background: active ? accent : "rgba(255,255,255,0.3)", boxShadow: active ? `0 0 10px ${accent}` : "none" }} />
        {label}
      </span>
    </button>
  );
}

// ─── Accordion row ───────────────────────────────────────────────
function FeatureRow({ feature, index, isActive, onActivate, accent }) {
  return (
    <button
      onClick={onActivate}
      className="feat-row w-full text-left relative transition-all duration-500"
      style={{
        padding: "24px 28px",
        borderRadius: 14,
        background: isActive ? `linear-gradient(135deg, ${accent}14, rgba(255,255,255,0.02))` : "transparent",
        border: isActive ? `1px solid ${accent}55` : "1px solid rgba(255,255,255,0.06)",
        marginBottom: 8,
      }}
    >
      {isActive && (
        <span
          className="absolute left-0 top-1/2 -translate-y-1/2"
          style={{
            width: 3,
            height: "60%",
            background: `linear-gradient(180deg, ${accent}, #ff8aa3)`,
            borderRadius: 2,
            boxShadow: `0 0 12px ${accent}`,
          }}
        />
      )}
      <div className="flex items-center gap-4">
        <div
          className="flex items-center justify-center shrink-0 transition-transform duration-500"
          style={{
            width: 44,
            height: 44,
            borderRadius: 11,
            background: isActive ? `${accent}22` : "rgba(255,255,255,0.04)",
            border: isActive ? `1px solid ${accent}44` : "1px solid rgba(255,255,255,0.08)",
            transform: isActive ? "rotate(-4deg) scale(1.08)" : "none",
          }}
        >
          <img src={feature.icon} alt="" style={{ width: 24, height: 24 }} />
        </div>
        <div className="flex-1 min-w-0">
          <div className="flex items-center gap-2 mb-1">
            <span style={{ fontSize: 10, fontWeight: 700, color: "rgba(255,255,255,0.35)", fontFamily: "ui-monospace, Menlo, monospace", letterSpacing: 1.5 }}>
              0{index + 1}
            </span>
            <span
              className="rounded-full uppercase"
              style={{
                fontSize: 9,
                fontWeight: 700,
                letterSpacing: 1.2,
                padding: "3px 8px",
                background: isActive ? `${accent}1a` : "rgba(255,255,255,0.05)",
                color: isActive ? accent : "rgba(255,255,255,0.45)",
                border: isActive ? `1px solid ${accent}44` : "1px solid rgba(255,255,255,0.08)",
              }}
            >
              {feature.tag}
            </span>
          </div>
          <div className="font-bold leading-tight" style={{ fontSize: 17, color: isActive ? "#fff" : "rgba(255,255,255,0.75)" }}>
            {feature.title}
          </div>
        </div>
        <ArrowRight size={18} style={{ color: isActive ? accent : "rgba(255,255,255,0.25)", transform: isActive ? "translateX(4px)" : "none", transition: "all .4s ease" }} />
      </div>
    </button>
  );
}

// ─── Preview panel ───────────────────────────────────────────────
function PreviewPanel({ feature, accent }) {
  return (
    <div
      key={feature.title}
      className="relative overflow-hidden feat-preview-enter"
      style={{
        borderRadius: 24,
        background: "linear-gradient(180deg, rgba(255,255,255,0.04), rgba(255,255,255,0.015))",
        border: "1px solid rgba(255,255,255,0.08)",
        padding: 40,
        minHeight: 560,
      }}
    >
      <div
        className="absolute pointer-events-none"
        style={{
          top: -120, right: -120, width: 360, height: 360,
          background: `radial-gradient(circle, ${accent}33, transparent 65%)`,
          filter: "blur(20px)",
        }}
      />
      <div
        className="absolute pointer-events-none"
        style={{
          bottom: -100, left: -100, width: 300, height: 300,
          background: "radial-gradient(circle, rgba(255,138,163,0.18), transparent 65%)",
          filter: "blur(30px)",
        }}
      />

      <div className="relative flex flex-col h-full" style={{ minHeight: 480 }}>
        <div className="flex items-center justify-between mb-8">
          <span
            className="rounded-full uppercase inline-flex items-center gap-2"
            style={{
              fontSize: 10, fontWeight: 700, letterSpacing: 1.8, padding: "6px 12px",
              background: `${accent}1a`, color: accent, border: `1px solid ${accent}44`,
            }}
          >
            <span className="rounded-full" style={{ width: 5, height: 5, background: accent, boxShadow: `0 0 8px ${accent}` }} />
            {feature.tag}
          </span>
          <div
            className="flex items-center justify-center"
            style={{
              width: 72, height: 72, borderRadius: 18,
              background: `linear-gradient(135deg, ${accent}22, rgba(255,138,163,0.12))`,
              border: `1px solid ${accent}55`,
            }}
          >
            <img src={feature.icon} alt="" style={{ width: 40, height: 40 }} />
          </div>
        </div>

        <h3 className="font-black text-white mb-4" style={{ fontSize: 36, lineHeight: 1.1, letterSpacing: -0.5 }}>
          {feature.title}
        </h3>

        <p className="text-white/70 mb-8" style={{ fontSize: 16, lineHeight: 1.55, maxWidth: 440 }}>
          {feature.description}
        </p>

        <div className="space-y-3 mb-auto">
          {feature.bullets.map((b, i) => (
            <div key={i} className="flex items-start gap-3">
              <div
                className="flex items-center justify-center shrink-0 mt-0.5"
                style={{
                  width: 20, height: 20, borderRadius: 6,
                  background: `${accent}22`, border: `1px solid ${accent}55`,
                }}
              >
                <Check size={12} style={{ color: accent, strokeWidth: 3 }} />
              </div>
              <span className="text-white/85" style={{ fontSize: 14, lineHeight: 1.5 }}>{b}</span>
            </div>
          ))}
        </div>

        <div
          className="mt-8 flex items-end justify-between"
          style={{
            padding: 20, borderRadius: 14,
            background: "rgba(0,0,0,0.35)",
            border: `1px solid ${accent}33`, backdropFilter: "blur(8px)",
          }}
        >
          <div>
            <div className="font-black" style={{ fontSize: 48, lineHeight: 1, color: accent, letterSpacing: -1.5 }}>
              {feature.stat.value}
            </div>
            <div className="text-white/60 uppercase mt-2" style={{ fontSize: 10, letterSpacing: 2, fontWeight: 700 }}>
              {feature.stat.label}
            </div>
          </div>
          <div className="flex items-center gap-1.5 text-white/50" style={{ fontSize: 11, fontWeight: 600 }}>
            <span className="rounded-full animate-pulse" style={{ width: 6, height: 6, background: "#22c55e" }} />
            Live metric
          </div>
        </div>
      </div>
    </div>
  );
}

// ─── MAIN BLOCK ──────────────────────────────────────────────────
export default function Block() {
  const [tab, setTab] = useState("shop");
  const [activeIdx, setActiveIdx] = useState(0);

  const isShop = tab === "shop";
  const features = isShop ? SHOP_FEATURES : LIVE_FEATURES;
  const accent = "#fe2c55";
  const active = features[activeIdx] || features[0];

  const switchTab = (t) => {
    setTab(t);
    setActiveIdx(0);
  };

  return (
    <section
      className="relative overflow-hidden w-full"
      style={{
        background: "#000624",
        fontFamily: "Inter, ui-sans-serif, system-ui, sans-serif",
        color: "white",
      }}
    >
      <link rel="preconnect" href="https://fonts.googleapis.com" />
      <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&display=swap" rel="stylesheet" />
      <style>{`
        @keyframes featV2Enter {
          0% { opacity: 0; transform: translateY(12px); }
          100% { opacity: 1; transform: translateY(0); }
        }
        .feat-preview-enter { animation: featV2Enter .45s ease; }
        .feat-row:hover { background: rgba(255,255,255,0.04); }
      `}</style>

      <div className="absolute inset-0 pointer-events-none" style={{
        background: "radial-gradient(ellipse at 20% 10%, rgba(254,44,85,0.14), transparent 55%), radial-gradient(ellipse at 90% 90%, rgba(255,138,163,0.1), transparent 55%)",
      }} />

      {/* ─── Desktop (1024+) ─── */}
      <div className="hidden lg:block relative" style={{ padding: "96px 64px" }}>
        <div className="mx-auto" style={{ maxWidth: 1140 }}>
          <div className="flex flex-col items-center text-center mb-14">
            <CreatorFeaturesBadge />
            <h2 className="font-black text-white mt-6 mb-4" style={{ fontSize: 68, lineHeight: 0.98, letterSpacing: -2, maxWidth: 900 }}>
              Land more deals,{" "}
              <span style={{
                fontFamily: "'Instrument Serif', serif",
                fontStyle: "italic",
                fontWeight: 400,
                color: "#ffb3c1",
                letterSpacing: -1.5,
              }}>
                that actually pay
              </span>
            </h2>
            <p className="text-white/65 mb-10" style={{ fontSize: 19, lineHeight: 1.5, maxWidth: 620 }}>
              Long-term partnerships with brands that reward performance — plus the tools to actually deliver. Switch between Shop and LIVE to explore what you get on each side.
            </p>

            {/* Tabs */}
            <div className="inline-flex items-center gap-2 p-1.5 rounded-full" style={{
              background: "rgba(255,255,255,0.03)",
              border: "1px solid rgba(255,255,255,0.08)",
            }}>
              <TabPill label="Shop" active={isShop} onClick={() => switchTab("shop")} accent="#fe2c55" />
              <TabPill label="LIVE" active={!isShop} onClick={() => switchTab("live")} accent="#fe2c55" />
            </div>
          </div>

          <div className="grid" style={{ gridTemplateColumns: "minmax(0, 1fr) minmax(0, 1.05fr)", gap: 48 }}>
            <div>
              {features.map((f, i) => (
                <FeatureRow
                  key={`${tab}-${i}`}
                  feature={f}
                  index={i}
                  isActive={activeIdx === i}
                  onActivate={() => setActiveIdx(i)}
                  accent={accent}
                />
              ))}
            </div>
            <div style={{ position: "sticky", top: 32, alignSelf: "start" }}>
              <PreviewPanel feature={active} accent={accent} />
            </div>
          </div>

          <div className="mt-16 flex items-center justify-center">
            <Button
              asChild
              className="rounded-full font-bold transition-all hover:scale-105 group"
              style={{
                background: accent,
                color: "white",
                padding: "18px 32px",
                fontSize: 15,
                height: "auto",
                boxShadow: `0 10px 30px -8px ${accent}88`,
              }}
            >
              <a href="/forms-creators" className="inline-flex items-center gap-2">
                {isShop ? "Join as a Shop creator" : "Apply as a LIVE creator"}
                <ArrowRight size={16} className="group-hover:translate-x-1 transition-transform" />
              </a>
            </Button>
          </div>
        </div>
      </div>

      {/* ─── Mobile (<1024) ─── */}
      <div className="block lg:hidden relative" style={{ padding: "72px 20px" }}>
        <div className="mx-auto" style={{ maxWidth: 640 }}>
          <div className="flex flex-col items-center text-center mb-10">
            <CreatorFeaturesBadge />
            <h2 className="font-black text-white mt-5 mb-3" style={{ fontSize: 40, lineHeight: 1, letterSpacing: -1.2 }}>
              Land more deals,{" "}
              <span style={{
                fontFamily: "'Instrument Serif', serif",
                fontStyle: "italic",
                fontWeight: 400,
                color: "#ffb3c1",
                letterSpacing: -1,
              }}>
                that actually pay
              </span>
            </h2>
            <p className="text-white/65 mb-7" style={{ fontSize: 15, lineHeight: 1.55 }}>
              Tap any capability to see how it works. Switch between Shop and LIVE.
            </p>

            <div className="inline-flex items-center gap-2 p-1.5 rounded-full" style={{
              background: "rgba(255,255,255,0.03)",
              border: "1px solid rgba(255,255,255,0.08)",
            }}>
              <TabPill label="Shop" active={isShop} onClick={() => switchTab("shop")} accent="#fe2c55" />
              <TabPill label="LIVE" active={!isShop} onClick={() => switchTab("live")} accent="#fe2c55" />
            </div>
          </div>

          <div>
            {features.map((f, i) => {
              const isActive = activeIdx === i;
              return (
                <div key={`${tab}-m-${i}`} className="mb-3">
                  <FeatureRow
                    feature={f}
                    index={i}
                    isActive={isActive}
                    onActivate={() => setActiveIdx(isActive ? -1 : i)}
                    accent={accent}
                  />
                  {isActive && (
                    <div className="mt-2" style={{ animation: "featV2Enter .4s ease" }}>
                      <PreviewPanel feature={f} accent={accent} />
                    </div>
                  )}
                </div>
              );
            })}
          </div>

          <div className="mt-10 flex items-center justify-center">
            <Button
              asChild
              className="rounded-full font-bold w-full max-w-xs"
              style={{
                background: accent,
                color: "white",
                padding: "16px 28px",
                fontSize: 14,
                height: "auto",
                boxShadow: `0 10px 30px -8px ${accent}88`,
              }}
            >
              <a href="/forms-creators" className="inline-flex items-center justify-center gap-2">
                {isShop ? "Join as a Shop creator" : "Apply as a LIVE creator"}
                <ArrowRight size={16} />
              </a>
            </Button>
          </div>
        </div>
      </div>
    </section>
  );
}
